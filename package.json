{
  "name": "bookreader-t06",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && yarn install --pure-lockfile && yarn start",
    "server": "cd server && yarn install --pure-lockfile && yarn start"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "fs": "^0.0.1-security",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
